if exists('g:loaded_defx') && g:loaded_defx == 1
    " auto update file tree
        " autocmd WinLeave * call defx#redraw()
        " autocmd CmdlineLeave * call defx#redraw()
        " autocmd BufWritePost * call defx#redraw()
endif
